name: Release

on:
  push:
    tags:
      - "v*.*.*"

env:
  REGISTRY: ghcr.io
  IMAGE: ghcr.io/${{ github.repository }}
  IMAGE_CLUSTER: ghcr.io/${{ github.repository }}-cluster

jobs:
  branch:
    name: Create release branch
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Install system dependencies
        run: sudo apt-get install -y libfuse3-dev libgtk-3-dev libappindicator3-dev

      - name: Bump workspace version to tag
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          sed -i "s/^version = \".*\"/version = \"${VERSION}\"/" Cargo.toml
          echo "VERSION=${VERSION}" >> "$GITHUB_ENV"

      - name: Refresh Cargo.lock
        run: cargo check --workspace --quiet

      - name: Commit + push release branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Cargo.toml Cargo.lock
          git diff --staged --quiet || git commit -m "chore(release): set version to ${VERSION}"
          git push origin HEAD:refs/heads/${{ github.ref_name }}

  build:
    name: Build (${{ matrix.target }})
    needs: branch
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-24.04
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-24.04
            cross_pkg: gcc-aarch64-linux-gnu
            linker_env: CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER
            linker: aarch64-linux-gnu-gcc
            cc_env: CC_aarch64_unknown_linux_gnu
          - target: riscv64gc-unknown-linux-gnu
            os: ubuntu-24.04
            cross_pkg: gcc-riscv64-linux-gnu
            linker_env: CARGO_TARGET_RISCV64GC_UNKNOWN_LINUX_GNU_LINKER
            linker: riscv64-linux-gnu-gcc
            cc_env: CC_riscv64gc_unknown_linux_gnu
          - target: x86_64-apple-darwin
            os: macos-latest
          - target: aarch64-apple-darwin
            os: macos-latest
          - target: x86_64-pc-windows-msvc
            os: windows-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.ref_name }}

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Install cross-compiler
        if: matrix.cross_pkg
        run: |
          sudo apt-get update
          sudo apt-get install -y ${{ matrix.cross_pkg }}
          echo "${{ matrix.linker_env }}=${{ matrix.linker }}" >> "$GITHUB_ENV"
          echo "${{ matrix.cc_env }}=${{ matrix.linker }}" >> "$GITHUB_ENV"

      - name: Install macFUSE
        if: runner.os == 'macOS'
        run: |
          brew install --cask macfuse
          echo "PKG_CONFIG_PATH=/usr/local/lib/pkgconfig:$PKG_CONFIG_PATH" >> "$GITHUB_ENV"

      - name: Install Linux GUI dependencies
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libgtk-3-dev libappindicator3-dev

      - name: Install WinFsp
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          # winfsp_wrs_sys's build script reads the WinFsp registry key and
          # links against its import lib at *build* time (not just runtime),
          # so this must run before any step that builds y2q-mount-windows or
          # y2q-gui.
          choco install winfsp -y --no-progress

      - name: Build release binaries
        run: cargo build --release --target ${{ matrix.target }} -p y2qd -p y2q-cli -p y2q-warp -p y2q-gui

      - name: Build FUSE binary
        if: runner.os == 'Linux' || runner.os == 'macOS'
        run: cargo build --release --target ${{ matrix.target }} -p y2q-fuse

      - name: Build Windows mount binary
        if: runner.os == 'Windows'
        run: cargo build --release --target ${{ matrix.target }} -p y2q-mount-windows

      - name: Package (Linux)
        if: runner.os == 'Linux'
        run: |
          mkdir -p dist
          tar czf dist/y2q-${{ github.ref_name }}-${{ matrix.target }}.tar.gz \
            -C target/${{ matrix.target }}/release y2qd y2q y2q-warp y2q-fuse y2q-gui

      - name: Package (macOS)
        if: runner.os == 'macOS'
        run: |
          mkdir -p dist
          tar czf dist/y2q-${{ github.ref_name }}-${{ matrix.target }}.tar.gz \
            -C target/${{ matrix.target }}/release y2qd y2q y2q-warp y2q-fuse y2q-gui

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force dist | Out-Null
          Compress-Archive -Path @(
            "target\${{ matrix.target }}\release\y2qd.exe",
            "target\${{ matrix.target }}\release\y2q.exe",
            "target\${{ matrix.target }}\release\y2q-warp.exe",
            "target\${{ matrix.target }}\release\y2q-gui.exe",
            "target\${{ matrix.target }}\release\y2q-mount-windows.exe"
          ) -DestinationPath "dist\y2q-${{ github.ref_name }}-${{ matrix.target }}.zip"

      - uses: actions/upload-artifact@v7
        with:
          name: y2q-${{ github.ref_name }}-${{ matrix.target }}
          path: dist/*

  docker-release:
    name: Build + push release images
    needs: branch
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.ref_name }}

      - name: Lowercase image names
        run: |
          echo "IMAGE=$(echo "$IMAGE" | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_ENV"
          echo "IMAGE_CLUSTER=$(echo "$IMAGE_CLUSTER" | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_ENV"

      - uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build + push y2q image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE }}:${{ github.ref_name }}
            ${{ env.IMAGE }}:latest

      - name: Build + push cluster image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: deploy/cluster/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_CLUSTER }}:${{ github.ref_name }}
            ${{ env.IMAGE_CLUSTER }}:latest

  release:
    name: Create GitHub Release
    needs: [build, branch]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/download-artifact@v8
        with:
          path: dist
          pattern: "y2q-${{ github.ref_name }}-*"
          merge-multiple: true

      - name: Generate checksums
        working-directory: dist
        run: sha256sum * > SHA256SUMS

      - name: Generate release notes
        run: |
          PREV_TAG=$(git tag --sort=-version:refname | grep -v "^${{ github.ref_name }}$" | head -n1)
          {
            echo "## Changes"
            if [ -n "$PREV_TAG" ]; then
              git log "${PREV_TAG}..${{ github.ref_name }}" --oneline --no-merges
            else
              git log "${{ github.ref_name }}" --oneline --no-merges
            fi
          } > notes.md

      - name: Create release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ github.ref_name }}" \
            dist/* \
            --title "${{ github.ref_name }}" \
            --notes-file notes.md
